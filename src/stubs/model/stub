import { Model } from '@formidablejs/framework'

export default class {{class}} < Model

	# The table associated with the model.
	#
	# @type {string}

	get tableName
		'{{table}}'

	# The attributes that should be hidden.
	#
	# @type {string[]}

	get hidden
		[]
