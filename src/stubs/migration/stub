const { Database } = require('@formidablejs/framework');

/** @param {Database} DB */
exports.up = (DB) => {
	return DB.schema.createTable('{{table}}', (table) => {
{{schema}}
	});
};

/** @param {Database} DB */
exports.down = (DB) => DB.schema.dropTable('{{table}}');
