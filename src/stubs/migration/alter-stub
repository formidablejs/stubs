const { Database } = require('@formidablejs/framework');

/** @param {Database} DB */
exports.up = (DB) => {
	return DB.schema.table('{{table}}', (table) => {

	});
};

/** @param {Database} DB */
exports.down = (DB) => {
	return DB.schema.table('{{table}}', (table) => {

	});
};
